"use client"

/**
 * Re-export store Pagination for use in car listing pages.
 * Use: <Pagination page={currentPage} totalPages={totalPages} />
 */
export { Pagination } from "@modules/store/components/pagination"
